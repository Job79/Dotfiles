# Basic tools
nvim 			# Editor

nnn 			# File manager feh				# Image viewer/background
neofetch		# System info
git				# Git
aria2           # Torrent client
scrot			# Screenshots
picom           # Compositor
hsetroot        # Background color - compositor
dvtm            # "Window manager" for the framebuffer

# Install WM : dwm 
xorg-server xorg-xinit dmenu
ttf-hack # Font

##! Create file "/etc/X11/xinit/xinitrc"
## with content: "exec dwm"
## Install compisotor (picom)

##! Append to ~/.profile to autostart dwm 
if [[ "$(tty)" == '/dev/tty1' ]]; then
    exec startx
fi
# gtk Themes
adapta-gtk-theme arc-icon-theme lxappearance

# Tools
firefox-developer-edition	# Browser

# Optional
## Audio
pulseaudio
pavucontrol 	# Gui for pulseaudio

## Multiple screens
xorg-xrandr
arandr			# Gui for xorg-xrandr

## Android phone
jmtpfs

## Install yay
fakeroot binutils make gcc	# Used for building packages
#-> git clone https://aur.archlinux.org/yay.git
#-> cd yay
#-> makepkg -si

# Powertop (Laptop only)
# Create file /etc/systemd/system/powertop.service
# with content: "
[Unit]
Description=Powertop tunings

[Service]
Type=exec
ExecStart=/usr/bin/powertop --auto-tune
RemainAfterExit=true

[Install]
WantedBy=multi-user.target
"

## Touchpad click on tap
# Create file /etc/X11/xorg.conf.d/90-touchpad.conf
# with content: "
Section "InputClass"
        Identifier "touchpad"
        MatchIsTouchpad "on"
        Driver "libinput"
        Option "Tapping" "on"
        Option "TappingButtonMap" "lrm"
        Option "ScrollMethod" "twofinger"
EndSection
"

# Multiple screens
xrandr --output HDMI-1 --auto --same-as eDP-1

# Fix java dwm
# Create file "/etc/profile.d/jre.sh"
# with content: "
export _JAVA_AWT_WM_NONREPARENTING=1
"
