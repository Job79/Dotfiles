#
# ~/.mkshrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Useful aliases
alias b='bash'
alias n='nnn'
alias c='clear'
alias ls='ls -CF --color=auto'
alias myip='curl ipv4.icanhazip.com'

# Monitor config
alias monitor_mirror='xrandr --output HDMI-1 --auto --same-as eDP-1'
alias monitor_on='xrandr --output HDMI-1 --auto --above eDP-1'
alias monitor_off='xrandr --output HDMI-1 --off'

# Set prompt
PS1=$'\E[36m$(if [[ "${PWD#$HOME}" = "$PWD" ]] then echo -n "$PWD"; else echo -n "~${PWD#$HOME}"; fi;)\E[0m ➤ '

# Enable history
HISTFILE="$HOME/.sh_history"
