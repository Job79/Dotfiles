# Useful aliases
alias n='nnn'
alias myip='curl api.ipify.org'
alias post='curl -F "sprunge=<-" http://sprunge.us'
d(){ if [ "${1::1}" == '-' ]; then n="${1:1}"; shift; else n="$1"; fi; dtach -A "$n" "$@"; }
